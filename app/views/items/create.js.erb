<% if @item.valid? %>
   $('.new-item').html("<%= escape_javascript(render partial: 'items/item', locals: { item: @new_item }) %>");
 <% else %>
   $('.new-item').html("<%= escape_javascript(render partial: 'items/item', locals: { item: @item }) %>");
 <% end %>